From 190ff653954ad44eeab68c01deb130d794e9dd33 Mon Sep 17 00:00:00 2001
From: mingzq <north_sea@qq.com>
Date: Thu, 24 Oct 2024 09:37:27 +0000
Subject: [PATCH 1/1] fix: v8 lite mode compile errors.

---
 deps/v8/src/builtins/riscv/builtins-riscv.cc  |  2 +
 .../codegen/riscv/macro-assembler-riscv.cc    |  6 +++
 .../backend/riscv/code-generator-riscv.cc     | 37 ++++++++++++-------
 .../riscv/instruction-scheduler-riscv.cc      |  4 ++
 .../riscv/instruction-selector-riscv.h        |  8 +++-
 .../riscv/instruction-selector-riscv64.cc     |  5 +++
 6 files changed, 48 insertions(+), 14 deletions(-)

diff --git a/deps/v8/src/builtins/riscv/builtins-riscv.cc b/deps/v8/src/builtins/riscv/builtins-riscv.cc
index 9ef4d5f8..2505e544 100644
--- a/deps/v8/src/builtins/riscv/builtins-riscv.cc
+++ b/deps/v8/src/builtins/riscv/builtins-riscv.cc
@@ -3267,6 +3267,7 @@ void Builtins::Generate_DoubleToI(MacroAssembler* masm) {
   __ Ret();
 }
 
+#if V8_ENABLE_WEBASSEMBLY
 void Builtins::Generate_WasmReturnPromiseOnSuspendAsm(MacroAssembler* masm) {
   __ Trap();
 }
@@ -3338,6 +3339,7 @@ void Builtins::Generate_WasmOnStackReplace(MacroAssembler* masm) {
 }
 
 void Builtins::Generate_JSToWasmWrapperAsm(MacroAssembler* masm) { __ Trap(); }
+#endif // V8_ENABLE_WEBASSEMBLY
 
 void Builtins::Generate_CallApiCallbackImpl(MacroAssembler* masm,
                                             CallApiCallbackMode mode) {
diff --git a/deps/v8/src/codegen/riscv/macro-assembler-riscv.cc b/deps/v8/src/codegen/riscv/macro-assembler-riscv.cc
index fd5e9733..95a8bfb8 100644
--- a/deps/v8/src/codegen/riscv/macro-assembler-riscv.cc
+++ b/deps/v8/src/codegen/riscv/macro-assembler-riscv.cc
@@ -23,7 +23,9 @@
 #include "src/objects/heap-number.h"
 #include "src/runtime/runtime.h"
 #include "src/snapshot/snapshot.h"
+#if V8_ENABLE_WEBASSEMBLY
 #include "src/wasm/wasm-code-manager.h"
+#endif
 
 // Satisfy cpplint check, but don't include platform-specific header. It is
 // included recursively via macro-assembler.h.
@@ -586,10 +588,14 @@ void MacroAssembler::CallRecordWriteStub(Register object, Register slot_address,
   // need to be caller saved.
   DCHECK_EQ(WriteBarrierDescriptor::ObjectRegister(), object);
   DCHECK_EQ(WriteBarrierDescriptor::SlotAddressRegister(), slot_address);
+#if V8_ENABLE_WEBASSEMBLY
   if (mode == StubCallMode::kCallWasmRuntimeStub) {
     auto wasm_target =
         static_cast<Address>(wasm::WasmCode::GetRecordWriteBuiltin(fp_mode));
     Call(wasm_target, RelocInfo::WASM_STUB_CALL);
+#else
+  if (false) {
+#endif
   } else {
     CallBuiltin(Builtins::RecordWrite(fp_mode));
   }
diff --git a/deps/v8/src/compiler/backend/riscv/code-generator-riscv.cc b/deps/v8/src/compiler/backend/riscv/code-generator-riscv.cc
index bb52536d..20610ae7 100644
--- a/deps/v8/src/compiler/backend/riscv/code-generator-riscv.cc
+++ b/deps/v8/src/compiler/backend/riscv/code-generator-riscv.cc
@@ -764,6 +764,7 @@ CodeGenerator::CodeGenResult CodeGenerator::AssembleArchInstruction(
       frame_access_state()->ClearSPDelta();
       break;
     }
+#if V8_ENABLE_WEBASSEMBLY
     case kArchCallWasmFunction: {
       if (instr->InputAt(0)->IsImmediate()) {
         Constant constant = i.ToConstant(instr->InputAt(0));
@@ -777,6 +778,20 @@ CodeGenerator::CodeGenResult CodeGenerator::AssembleArchInstruction(
       frame_access_state()->ClearSPDelta();
       break;
     }
+    case kArchTailCallWasm: {
+      if (instr->InputAt(0)->IsImmediate()) {
+        Constant constant = i.ToConstant(instr->InputAt(0));
+        Address wasm_code = static_cast<Address>(constant.ToInt64());
+        __ Jump(wasm_code, constant.rmode());
+      } else {
+        __ AddWord(kScratchReg, i.InputOrZeroRegister(0), 0);
+        __ Jump(kScratchReg);
+      }
+      frame_access_state()->ClearSPDelta();
+      frame_access_state()->SetFrameAccessToDefault();
+      break;
+    }
+#endif  // V8_ENABLE_WEBASSEMBLY
     case kArchTailCallCodeObject: {
       if (instr->InputAt(0)->IsImmediate()) {
         __ Jump(i.InputCode(0), RelocInfo::CODE_TARGET);
@@ -793,19 +808,6 @@ CodeGenerator::CodeGenResult CodeGenerator::AssembleArchInstruction(
       frame_access_state()->SetFrameAccessToDefault();
       break;
     }
-    case kArchTailCallWasm: {
-      if (instr->InputAt(0)->IsImmediate()) {
-        Constant constant = i.ToConstant(instr->InputAt(0));
-        Address wasm_code = static_cast<Address>(constant.ToInt64());
-        __ Jump(wasm_code, constant.rmode());
-      } else {
-        __ AddWord(kScratchReg, i.InputOrZeroRegister(0), 0);
-        __ Jump(kScratchReg);
-      }
-      frame_access_state()->ClearSPDelta();
-      frame_access_state()->SetFrameAccessToDefault();
-      break;
-    }
     case kArchTailCallAddress: {
       CHECK(!instr->InputAt(0)->IsImmediate());
       Register reg = i.InputOrZeroRegister(0);
@@ -4316,10 +4318,15 @@ void CodeGenerator::AssembleConstructFrame() {
 
   if (frame_access_state()->has_frame()) {
     if (call_descriptor->IsCFunctionCall()) {
+#if V8_ENABLE_WEBASSEMBLY
       if (info()->GetOutputStackFrameType() == StackFrame::C_WASM_ENTRY) {
         __ StubPrologue(StackFrame::C_WASM_ENTRY);
         // Reserve stack space for saving the c_entry_fp later.
         __ SubWord(sp, sp, Operand(kSystemPointerSize));
+#else
+      // For balance.
+      if (false) {
+#endif  // V8_ENABLE_WEBASSEMBLY
       } else {
         __ Push(ra, fp);
         __ Move(fp, sp);
@@ -4328,6 +4335,7 @@ void CodeGenerator::AssembleConstructFrame() {
       __ Prologue();
     } else {
       __ StubPrologue(info()->GetOutputStackFrameType());
+#if V8_ENABLE_WEBASSEMBLY
       if (call_descriptor->IsWasmFunctionCall() ||
           call_descriptor->IsWasmImportWrapper() ||
           call_descriptor->IsWasmCapiFunction()) {
@@ -4354,6 +4362,7 @@ void CodeGenerator::AssembleConstructFrame() {
         // Reserve space for saving the PC later.
         __ SubWord(sp, sp, Operand(kSystemPointerSize));
       }
+#endif  // V8_ENABLE_WEBASSEMBLY
     }
   }
 
@@ -4378,6 +4387,7 @@ void CodeGenerator::AssembleConstructFrame() {
 
   if (required_slots > 0) {
     DCHECK(frame_access_state()->has_frame());
+#if V8_ENABLE_WEBASSEMBLY
     if (info()->IsWasm() && required_slots > 128) {
       // For WebAssembly functions with big frames we have to do the stack
       // overflow check before we construct the frame. Otherwise we may not
@@ -4410,6 +4420,7 @@ void CodeGenerator::AssembleConstructFrame() {
 
       __ bind(&done);
     }
+#endif  // V8_ENABLE_WEBASSEMBLY
   }
 
   const int returns = frame()->GetReturnSlotCount();
diff --git a/deps/v8/src/compiler/backend/riscv/instruction-scheduler-riscv.cc b/deps/v8/src/compiler/backend/riscv/instruction-scheduler-riscv.cc
index c93fe5a7..6b35119f 100644
--- a/deps/v8/src/compiler/backend/riscv/instruction-scheduler-riscv.cc
+++ b/deps/v8/src/compiler/backend/riscv/instruction-scheduler-riscv.cc
@@ -1087,10 +1087,14 @@ int InstructionScheduler::GetInstructionLatency(const Instruction* instr) {
   // numbers).
   switch (instr->arch_opcode()) {
     case kArchCallCodeObject:
+#if V8_ENABLE_WEBASSEMBLY
     case kArchCallWasmFunction:
+#endif  // V8_ENABLE_WEBASSEMBLY
       return CallLatency();
     case kArchTailCallCodeObject:
+#if V8_ENABLE_WEBASSEMBLY
     case kArchTailCallWasm:
+#endif  // V8_ENABLE_WEBASSEMBLY
     case kArchTailCallAddress:
       return JumpLatency();
     case kArchCallJSFunction: {
diff --git a/deps/v8/src/compiler/backend/riscv/instruction-selector-riscv.h b/deps/v8/src/compiler/backend/riscv/instruction-selector-riscv.h
index c319800a..b302e544 100644
--- a/deps/v8/src/compiler/backend/riscv/instruction-selector-riscv.h
+++ b/deps/v8/src/compiler/backend/riscv/instruction-selector-riscv.h
@@ -398,6 +398,7 @@ template <typename Adapter>
 void EmitS128Load(InstructionSelectorT<Adapter>* selector, Node* node,
                   InstructionCode opcode, VSew sew, Vlmul lmul);
 
+#if V8_ENABLE_WEBASSEMBLY
 template <typename Adapter>
 void InstructionSelectorT<Adapter>::VisitLoadTransform(node_t node) {
   if constexpr (Adapter::IsTurboshaft) {
@@ -508,6 +509,7 @@ void InstructionSelectorT<Adapter>::VisitLoadTransform(node_t node) {
     }
   }
 }
+#endif  // V8_ENABLE_WEBASSEMBLY
 
 // Shared routine for multiple compare operations.
 template <typename Adapter>
@@ -1139,6 +1141,7 @@ void InstructionSelectorT<TurbofanAdapter>::VisitWord32Sar(Node* node) {
   VisitRRO(this, kRiscvSar32, node);
 }
 
+#if V8_ENABLE_WEBASSEMBLY
 template <typename Adapter>
 void InstructionSelectorT<Adapter>::VisitI32x4ExtAddPairwiseI16x8S(
     node_t node) {
@@ -2195,6 +2198,7 @@ void InstructionSelectorT<Adapter>::VisitI16x8UConvertI8x16Low(node_t node) {
          g.UseImmediate(m1));
   }
 }
+#endif // V8_ENABLE_WEBASSEMBLY
 
 template <typename Adapter>
 void InstructionSelectorT<Adapter>::VisitSignExtendWord8ToInt32(node_t node) {
@@ -2251,6 +2255,7 @@ InstructionSelectorT<TurboshaftAdapter>::FindProjection(
   UNIMPLEMENTED();
 }
 
+#if V8_ENABLE_WEBASSEMBLY
 #define VISIT_EXT_MUL(OPCODE1, OPCODE2, TYPE)                                 \
   template <typename Adapter>                                                 \
   void InstructionSelectorT<Adapter>::Visit##OPCODE1##ExtMulLow##OPCODE2##S(  \
@@ -2357,6 +2362,7 @@ void InstructionSelectorT<Adapter>::VisitF64x2Pmax(node_t node) {
     VisitUniqueRRR(this, kRiscvF64x2Pmax, node);
   }
 }
+#endif // V8_ENABLE_WEBASSEMBLY
 
 // static
 MachineOperatorBuilder::AlignmentRequirements
@@ -2391,7 +2397,7 @@ void InstructionSelectorT<Adapter>::VisitSetStackPointer(node_t node) {
   Emit(kArchSetStackPointer | MiscField::encode(fp_scope), 0, nullptr, 1,
        &input);
 }
-#endif
+#endif // V8_ENABLE_WEBASSEMBLY
 }  // namespace compiler
 }  // namespace internal
 }  // namespace v8
diff --git a/deps/v8/src/compiler/backend/riscv/instruction-selector-riscv64.cc b/deps/v8/src/compiler/backend/riscv/instruction-selector-riscv64.cc
index 4807682a..23e0aa51 100644
--- a/deps/v8/src/compiler/backend/riscv/instruction-selector-riscv64.cc
+++ b/deps/v8/src/compiler/backend/riscv/instruction-selector-riscv64.cc
@@ -326,6 +326,7 @@ void EmitS128Load(InstructionSelectorT<Adapter>* selector, Node* node,
   }
 }
 
+#if V8_ENABLE_WEBASSEMBLY
 template <typename Adapter>
 void InstructionSelectorT<Adapter>::VisitStoreLane(node_t node) {
   if constexpr (Adapter::IsTurboshaft) {
@@ -381,6 +382,7 @@ void InstructionSelectorT<Adapter>::VisitLoadLane(node_t node) {
          g.UseImmediate(params.laneidx), addr_reg, g.TempImmediate(0));
   }
 }
+#endif  // V8_ENABLE_WEBASSEMBLY
 
 template <typename Adapter>
 void InstructionSelectorT<Adapter>::VisitLoad(node_t node) {
@@ -3208,6 +3210,7 @@ void InstructionSelectorT<Adapter>::VisitSignExtendWord32ToInt64(node_t node) {
   }
 }
 
+#if V8_ENABLE_WEBASSEMBLY
 template <typename Adapter>
 void InstructionSelectorT<Adapter>::VisitF64x2Min(node_t node) {
   if constexpr (Adapter::IsTurboshaft) {
@@ -3267,6 +3270,8 @@ void InstructionSelectorT<Adapter>::VisitF64x2Max(node_t node) {
                g.UseImmediate(m1));
   }
 }
+#endif // V8_ENABLE_WEBASSEMBLY
+
 // static
 MachineOperatorBuilder::Flags
 InstructionSelector::SupportedMachineOperatorFlags() {
-- 
2.25.1

